import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        background: {
            default: "#212529",
        },
        text: {
            // 7d8792
            primary: "#F4EEE0",
        },
    },
});

export default theme;